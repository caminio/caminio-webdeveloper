<section class="main-wrapper">
  <header>
    <div class="box title-box website">
      <div class="main-ctrl">
        {{!-- <button class="btn icon-text" {{action 'promptNewWebpage'}}>
          <i class="fa fa-upload"></i>
          {{t 'media.upload'}}
        </button> --}}
      </div>
      <h1>{{t 'media_manager.title'}}</h1>

    </div>

  </header>

  {{media-manager labels=labels
                  webpages=webpages}}

</section>